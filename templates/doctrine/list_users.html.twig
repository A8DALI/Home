{% extends 'base.html.twig' %}

{% block body %}

    <h1>Users</h1>

    <table>
        <tr>
            <th>Id</th>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Email</th>
            <th>DdN</th>
        </tr>

        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.lastname }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.birthdate  ? user.birthdate|date('d.m.Y') }}</td>
            </tr>
        {% endfor %}
    </table>

{% endblock %}

